const coffee = document.querySelector(".coffee__mug");

window.addEventListener("DOMContentLoaded", () => {
	setTimeout(() => {
		coffee.style.opacity = "1";
	}, 500);
	// coffee.style.opacity = "1";
});
